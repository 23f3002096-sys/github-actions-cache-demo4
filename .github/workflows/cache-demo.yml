name: Cache Demo
on: [push]
jobs:
  demo:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    - name: prime-cache-23556f4
      id: cache-step
      uses: actions/cache@v4
      with:
        path: ~/.cache/demo
        key: cache-23556f4
    - name: Show result
      run: echo "Cache result: ${{ steps.cache-step.outputs.cache-hit }}"

